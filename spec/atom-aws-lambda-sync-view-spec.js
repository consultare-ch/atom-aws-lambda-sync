'use babel';

import AtomAwsLambdaSyncView from '../lib/atom-aws-lambda-sync-view';

describe('AtomAwsLambdaSyncView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
